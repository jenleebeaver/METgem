#!/usr/bin/env bash
#this loads dependencies for us 
#the only thing we would need to add here is other files or data we need to parse 
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here
